<div id="modal-decision-edit-form" class="modal-dialog" role="dialog" aria-labelledby="modalLabel" aria-hidden="true">
    <div class="modal-content">
        <div class="modal-header">
            <button type="button" class="close" data-dismiss="modal" aria-hidden="true">
                &times;
            </button>
            <h3 class="modal-title">
                Modification de la décision
            </h3>
        </div>
        <div class="modal-body">
            <form id="decision-edit-form"
                  action="{{ path('laurentBulletinUserDecisionEdit', {'decision': decision.getId()}) }}"
                  method="POST"
                  {{ form_enctype(form) }}
            > 
                {{ form_widget(form) }}
            </form>
        </div>
        <div class="modal-footer">
            <span class="btn btn-default" data-dismiss="modal">
                Annuler
            </span>
            <button type="submit" id='edit-btn' class="btn btn-primary">
                OK
            </button>
        </div>
    </div>
</div>